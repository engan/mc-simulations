{
	"devDependencies": {
		"apexcharts": "^4.5.0",
		"vue3-apexcharts": "^1.8.0",
		"wrangler": "^4.10.0"
	},
	"scripts": {
		"build:wasm": "wasm-pack build --target web --out-dir ./frontend/src/rust/pkg",
		"dev": "pnpm --filter frontend run dev"
	}
}
